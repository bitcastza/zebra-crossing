stages:
    - test
    - deploy

test:unit-3:
    script:
        - apt-get update -qq && apt-get install -y -qq python3 python3-django
        - python3 --version
        - python3 -m django --version
        - python3 ./manage.py test --parallel 5

deploy:staging:
    stage: deploy
    script:
        - apt-get install -y -qq ansible git stow
        - ./scripts/deploy.sh
    environment:
        name: staging
        url: https://zebratest.paddatrapper.com
    only:
        - master
